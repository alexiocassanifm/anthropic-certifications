---
id: "5.3"
domain: 5
domain_weight: 15
title: "Implement error propagation strategies across multi-agent systems"
scenarios: [3]
concepts: [structured-error-context, access-failure-vs-empty-result, local-error-recovery, coverage-annotations]
verified: "2026-08-12"
---

# 5.3 — Implement error propagation strategies across multi-agent systems

> Task statement title reproduced from *Exam Guide v1.0*. Everything below is this project's own explanation.

**Domain:** [5 — Context Management & Reliability](../domains/5-context-management-and-reliability.md) (15%)

## What it tests

What a coordinator needs from a failing subagent in order to recover intelligently.

## Knowledge

**Structured error context enables intelligent recovery.** Four things travel with the failure:

1. **Failure type**
2. **What was attempted** — the query
3. **Partial results**
4. **Potential alternative approaches**

With those, the coordinator can retry with a modified query, try a different approach, or proceed with partial results and annotate the gap. With `"search unavailable"`, it can do none of them.

**Access failures versus valid empty results.** A timeout needs a retry decision. A successful query with no matches is information. Flattening them destroys the coordinator's ability to choose.

**Generic error statuses hide valuable context** from the coordinator, preventing informed decisions.

**Two bracketing anti-patterns:** silently suppressing errors (returning empty results as success) and terminating entire workflows on a single failure. Both are wrong; the answer lives between them.

## Skills

- Return structured error context including failure type, what was attempted, partial results, and potential alternatives.
- Distinguish access failures from valid empty results in error reporting.
- **Have subagents implement local recovery for transient failures**, propagating only errors they cannot resolve — including what was attempted and any partial results.
- **Structure synthesis output with coverage annotations** indicating which findings are well-supported and which topic areas have gaps due to unavailable sources.

## Anti-patterns

- **Generic status strings** — `"search unavailable"` after exhausting retries. Hides everything the coordinator needs.
- **Catching a timeout and returning an empty result set marked successful.** Suppresses the error, prevents any recovery, and silently produces incomplete output. The worst of the four.
- **Propagating a timeout exception to a top-level handler that terminates the workflow.** Kills work that could have succeeded.
- **Propagating every transient blip** instead of recovering locally first.
- **Synthesis that does not say what is missing.** A report with a silent gap reads as complete.

## Exam angle

The canonical stem: *"The web search subagent times out while researching a complex topic. You need to design how this failure information flows back to the coordinator. Which error propagation approach best enables intelligent recovery?"*

Answer: structured error context — failure type, attempted query, partial results, potential alternatives.

The distractor set is unusually instructive because each one is a *different* failure mode:

| Distractor | Failure mode |
|---|---|
| Retry with exponential backoff, then return generic `"search unavailable"` | Hides context; the coordinator cannot make an informed decision |
| Catch the timeout and return an empty result set marked successful | **Suppresses** the error; prevents recovery; risks silently incomplete output |
| Propagate the exception to a top-level handler that terminates the workflow | Terminates unnecessarily when recovery could have succeeded |

Distractor families: `suppresses-signal`, `solves-different-problem`.

## Related

Concepts: [structured error context](../concepts/structured-error-context.md) · [access failure vs empty result](../concepts/access-failure-vs-empty-result.md) · [local error recovery](../concepts/mcp-errors.md)

Task statements: [2.2](2-2.md) the tool-level version of the same idea · [1.2](1-2.md) the coordinator's recovery role · [5.6](5-6.md) reporting gaps in synthesis

Scenario: [3](../scenarios/3-multi-agent-research-system.md)
